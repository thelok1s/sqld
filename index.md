# sqld

Ниже готовый набор для PostgreSQL. Во всех трёх таблицах будет по 10 связанных записей, предметная область — владельцы собак и посещения груминг-салона.

## 1. Таблица owners

Поля предполагаются такие:

```sql
owner_id SERIAL PRIMARY KEY,
first_name VARCHAR(50),
last_name VARCHAR(50)
```

Заполнение:

```sql
INSERT INTO owners (first_name, last_name) VALUES
('Алексей', 'Иванов'),
('Мария', 'Петрова'),
('Дмитрий', 'Соколов'),
('Анна', 'Кузнецова'),
('Максим', 'Попов'),
('Елена', 'Васильева'),
('Иван', 'Смирнов'),
('Ольга', 'Морозова'),
('Артём', 'Волков'),
('Наталья', 'Фёдорова');
```

## 2. Таблица dogs

Поля:

```sql
dog_id SERIAL PRIMARY KEY,
name VARCHAR(50),
breed VARCHAR(100),
owner_id INTEGER REFERENCES owners(owner_id)
```

Заполнение. `owner_id` соответствует владельцам выше:

```sql
INSERT INTO dogs (name, breed, owner_id) VALUES
('Боня', 'Золотистый ретривер', 1),
('Рекс', 'Немецкая овчарка', 2),
('Лайма', 'Лабрадор-ретривер', 3),
('Тоби', 'Корги', 4),
('Джек', 'Джек-рассел-терьер', 5),
('Белла', 'Шпиц', 6),
('Барон', 'Доберман', 7),
('Луна', 'Хаски', 8),
('Чарли', 'Пудель', 9),
('Грей', 'Французский бульдог', 10);
```

## 3. Таблица visits

Поля:

```sql
visit_id SERIAL PRIMARY KEY,
dog_id INTEGER REFERENCES dogs(dog_id),
visit_date DATE,
service VARCHAR(100)
```

Заполнение:

```sql
INSERT INTO visits (dog_id, visit_date, service) VALUES
(1, '2026-09-21', 'Комплексный уход'),
(2, '2026-09-21', 'Стрижка когтей'),
(3, '2026-09-22', 'Мытьё и сушка'),
(4, '2026-09-22', 'Гигиеническая стрижка'),
(5, '2026-09-22', 'Чистка зубов'),
(6, '2026-09-23', 'Мытьё и вычёсывание'),
(7, '2026-09-23', 'Стрижка когтей'),
(8, '2026-09-23', 'Комплексный уход'),
(9, '2026-09-24', 'Модельная стрижка'),
(10, '2026-09-24', 'Гигиеническая стрижка');
```

## Проверка

Для отчёта можно выполнить:

```sql
SELECT * FROM owners;

SELECT * FROM dogs;

SELECT * FROM visits;
```

А чтобы наглядно показать связанные данные одной командой:

```sql
SELECT
    o.first_name,
    o.last_name,
    d.name AS dog_name,
    d.breed,
    v.visit_date,
    v.service
FROM owners o
JOIN dogs d ON d.owner_id = o.owner_id
JOIN visits v ON v.dog_id = d.dog_id
ORDER BY d.dog_id;
```

Получится примерно такая логика:

- Алексей Иванов → Боня → Золотистый ретривер → 2026-09-21 → Комплексный уход
- Мария Петрова → Рекс → Немецкая овчарка → 2026-09-21 → Стрижка когтей
- Дмитрий Соколов → Лайма → Лабрадор-ретривер → 2026-09-22 → Мытьё и сушка
- ...

Для текста отчёта названия лучше привести к единому варианту: `owners → dogs → visits`, где `owner_id` связывает владельца с собакой, а `dog_id` — собаку с посещением груминг-салона.
